import React from 'react';
import { Outlet } from '@tanstack/react-router';
import './HeroDatabase.css';

const HeroDatabase: React.FC = () => {
    return <Outlet />;
};

export default HeroDatabase;

